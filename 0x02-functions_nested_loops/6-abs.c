#include "main.h"
/**
 * description: computes the absolute return value
 * @i: input number as an integer
 * return: the absolute value
 */
int _abs(int)
{
	if (i >= 0)
	{
		return (i);
	}
	else 
	{
		return (i * -1);
	}
}
