#include "main.h"
#include <stdio.h>

/**
 * mul: - multiplies the two integers
 * @a - first integer
 * @b - second integer
 * return: always 0
 */

int mul(int a, int b)
{
	int result = a * b;
	return (result);
}
