#include "main.h"
/**
 * _is lower -shows 1 if the input is a
 * lowercase character.
 * another case will return 0
 * return: 1 for lowercase character. 0 for others
 */
int _islower(int c)
{
	if  (c >= 97 && c <= 122)
	{
		return (0);
	}
	else
	{
		return (0);
	}
	_putchar('\n');
}
